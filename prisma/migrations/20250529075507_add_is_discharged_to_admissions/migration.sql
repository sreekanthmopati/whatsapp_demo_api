BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Admissions] ADD [is_discharged] BIT NOT NULL CONSTRAINT [Admissions_is_discharged_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
